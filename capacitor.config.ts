import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.skyaviator.skyaviator',
  appName: 'aviatorPlay',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
